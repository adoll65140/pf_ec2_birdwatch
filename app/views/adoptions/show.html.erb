<div class="col-md-10 mx-auto">
  <h2>里親の募集</h2>
</div>
<div class="col-md-10 mx-auto">
      <div class="card shadow p-5">
        
          
            <div class="card-content">
  <h3 class="card-title font-weight-bold p-2"><%= @adoption.title %></h3>
  <p>募集主：<%= link_to @adoption.user.nickname, user_path(@adoption.user) %></p>
  </div>
  <div class="col-lg-6 p-1">
  <%= attachment_image_tag @adoption, :image, :fill, 200, 200 %>
  </div>
    <p>
      年齢：<%= @adoption.age %>歳　　　性別：<%= @adoption.sex %>
    </p>
    <p>種類：<%= @adoption.breed.name %></p>

  <p>特徴　性格：<%= @adoption.introduction %></p>
  <p>健康状態：<%= @adoption.health %></p>
  <p>その他：<%= @adoption.other %></p>
  <p>募集経緯：<%= @adoption.background %></p>

  <p>地域：<%= @adoption.prefecture_name %><%= @adoption.address_city %></p>

  <% if user_signed_in? %>
  <% if @adoption.user.id == current_user.id %>
    <%= link_to "編集", edit_adoption_path(@adoption.id), class: "btn btn-success shadow" %>
  <% else %>
    <% if @isRoom == true %>
        <p><%= link_to 'DMへ', room_path(@roomId), class: "btn btn-success shadow" %></p>
    <% else %>
        <%= form_for @room do |f| %>
            <%= fields_for @entry do |e|%>
                <% e.hidden_field :user_id, value: @adoption.user.id %>
            <% end %>
        <%= f.submit "DMを開始する", class: "btn btn-success shadow"%>
        <% end %>
    <% end %>
<% end %>
<% end %>

</div>
</div>